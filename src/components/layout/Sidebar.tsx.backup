import { useState } from 'react';
import { Home, Users, BarChart2, Settings, Clock } from 'lucide-react';
import { cn } from '@/utils/cn';
import { PendingSearchesDialog } from '@/components/dashboard/PendingSearchesDialog';

interface SidebarProps {
    activeTab: string;
    onTabChange: (tab: string) => void;
}

export const Sidebar = ({ activeTab, onTabChange }: SidebarProps) => {
    const [showPendingDialog, setShowPendingDialog] = useState(false);

    const menuItems = [
        { id: 'dashboard', label: 'Dashboard', icon: Home },
        { id: 'leads', label: 'Leads', icon: Users },
        { id: 'analytics', label: 'Analiz', icon: BarChart2 },

    ];

    return (
        <>
            <aside className="fixed left-0 top-0 h-screen w-64 bg-card border-r border-border z-30">
                <div className="p-6">
                    <h1 className="text-2xl font-bold text-black dark:text-white">
                        LUERA
                    </h1>
                </div>

                <nav className="px-4 space-y-2">
                    {menuItems.map((item) => {
                        const Icon = item.icon;
                        return (
                            <button
                                key={item.id}
                                type="button"
                                onClick={() => onTabChange(item.id)}
                                className={cn(
                                    "w-full flex items-center space-x-3 px-4 py-3 rounded-lg transition-all duration-200",
                                    activeTab === item.id
                                        ? "bg-primary/10 text-primary font-medium"
                                        : "text-muted-foreground hover:bg-secondary hover:text-foreground"
                                )}
                            >
                                <Icon size={20} />
                                <span>{item.label}</span>
                            </button>
                        );
                    })}

                    {/* Pending Searches Button - Placed under Messages */}
                    <button
                        type="button"
                        onClick={() => setShowPendingDialog(true)}
                        className="w-full flex items-center space-x-3 px-4 py-3 rounded-lg transition-all duration-200 text-muted-foreground hover:bg-secondary hover:text-foreground"
                    >
                        <Clock size={20} />
                        <span>Bekleyen Aramalar</span>
                    </button>

                    {/* Settings at the bottom or separate group */}
                    <div className="pt-4 mt-4 border-t border-border">
                        <button
                            type="button"
                            onClick={() => onTabChange('settings')}
                            className={cn(
                                "w-full flex items-center space-x-3 px-4 py-3 rounded-lg transition-all duration-200",
                                activeTab === 'settings'
                                    ? "bg-primary/10 text-primary font-medium"
                                    : "text-muted-foreground hover:bg-secondary hover:text-foreground"
                            )}
                        >
                            <Settings size={20} />
                            <span>Ayarlar</span>
                        </button>
                    </div>
                </nav>

                <div className="absolute bottom-0 w-full p-6 border-t border-border">
                    <div className="flex items-center space-x-3">
                        <div className="w-10 h-10 rounded-full bg-secondary flex items-center justify-center">
                            <span className="font-bold text-primary">FU</span>
                        </div>
                        <div>
                            <p className="text-sm font-medium">Furkan Ulger</p>
                            <p className="text-xs text-muted-foreground">Pro Plan</p>
                        </div>
                    </div>
                </div>
            </aside>

            <PendingSearchesDialog
                open={showPendingDialog}
                onOpenChange={setShowPendingDialog}
            />
        </>
    );
};
